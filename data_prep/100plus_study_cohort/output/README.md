This folder is populated by running `../1_combine_snps_alleles_neuropathology_100plus.R` with access to the
individual-level data described in `../1_combine_snps_alleles_neuropathology_100plus.R`'s header. Its only output,
`hla_alleles_snps_dosages_microglia_neuropathology_100plus.csv`, is individual-level (one row per participant), so it is
not committed to this repository — see `DATA_ACCESS.md` at the repository root.
