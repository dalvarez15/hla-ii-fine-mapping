This folder is populated by running `../1_snps_phenotypes.R` then `../2_combine_snps_alleles_neuropathology_nbb.R` with access to
the individual-level data described in those scripts' headers. Its final output,
`hla_alleles_snps_dosages_neuropathology_nbb.csv`, is individual-level (one row per participant), so it is not
committed to this repository — see `DATA_ACCESS.md` at the repository root.
